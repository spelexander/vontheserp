import React from 'react';
import { styled } from '@material-ui/core/styles';
import Chip from '@material-ui/core/Chip';

export const StyledElementChip = styled(Chip)({
    marginTop: '4px',
    margin: '3px',
});